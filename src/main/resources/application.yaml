spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://autorepair-db-1:5432/auto_db
    username: oleg
    password: XnBX#kE9
  flyway:
    baseline-on-migrate: true
    baseline-version: 1
    schemas: core

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  mvc:
    hiddenmethod:
      filter:
        enabled: true
  freemarker:
    expose-request-attributes: true

application:
  upload:
    path: C:/Data/img/