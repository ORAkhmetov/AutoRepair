version: '3'

services:
  web:
    image: akhmetovor/auto_repair:latest
    ports:
      - "80:8080"
    depends_on:
      - db

  db:
    image: postgres:15.5
    environment:
      POSTGRES_DB: auto_db
      POSTGRES_USER: oleg
      POSTGRES_PASSWORD: XnBX#kE9
    ports:
      - "5432:5432"
